{
  "name": "shell",
  "version": "0.0.1",
  "description": "Web based view of SFCoin.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "scripts": {
    "build-sass": "node-sass --include-path scss ./styles/app.scss ./styles/app.css",
    "watch-sass": "nodemon -e scss -x \"npm run build-sass\""
  },
  "author": "Shota Shimazu",
  "license": "ISC",
  "devDependencies": {
    "node-sass": "^4.5.2",
    "nodemon": "^1.11.0"
  }
}
